# 과학 시뮬레이션 프로젝트 (하네스)

중학교 과학 개념을 눈으로 확인하는 웹 시뮬레이션 모음. 작성자 표기는 "두리쌤".
지금까지의 과정은 [docs/PROCESS.md](docs/PROCESS.md)에 정리되어 있다.

## 구조

- 루트가 GitHub repo(`mrdoolee/science-simulation-project`). 시뮬레이션은 폴더 하나에 하나씩 넣는다.
- `friction-electricity/` : 마찰전기 (배포: https://friction-electricity.vercel.app)
  - `index.html` 화면 뼈대(상단/중앙/하단 3층), 푸터 크레딧, 모달. **새 시뮬레이션의 기본 틀**
  - `style.css` 디자인 토큰(`:root` 변수)과 공통 컴포넌트 스타일. **새 시뮬레이션은 이 파일을 복사해서 시작**
  - `main.js` 실험 2(풍선과 털가죽) + 공용 도우미(`el`, `chargeMark`, `place`, `latticeSites`, `createCharges`)
  - `observe.js` 실험 1(마찰 전기 현상 관찰하기). `main.js`의 공용 도우미를 재사용
  - `app.js` 실험 메뉴 전환, 제작자 모달
- `design-proposals/` : 디자인 시안 보관용(실험 노트, 미션 스티커, 교과서 편집). 배포하지 않고 참고만 한다.
- `.claude/` : `launch.json`(로컬 미리보기), `settings.json` + `hooks/`(하네스)

## 기술 원칙

- 빌드 도구, 프레임워크, 외부 JS 라이브러리를 쓰지 않는다. 바닐라 JS + Canvas(왼쪽) + SVG(오른쪽).
- 각 시뮬레이션은 폴더 안에서 독립적으로 동작하는 정적 사이트여야 한다.
- 새 파일의 전역 이름이 기존 `main.js`와 충돌하지 않게 주의한다(IIFE로 감싼다).
- 비밀값(API 키, 토큰)을 코드나 커밋에 넣지 않는다.

## 디자인 규칙 (기본값)

새 시뮬레이션과 모든 수정은 아래를 **기본값**으로 따른다. 기준 구현은 `friction-electricity/`(`index.html` 구조, `style.css` 토큰과 컴포넌트).
방향은 "교과서 편집"(C안)의 색·분위기·구성에 "팝 스티커"(B안)의 버튼 형태를 더한 것이고, 화면 기준은 태블릿(768~1180px)이다. 대상은 중학생.

### 토큰 (`style.css` `:root`, 이 변수만 쓴다)

| 이름 | 값 | 쓰임 |
|---|---|---|
| `--paper` | `#f3f6fc` | 페이지 배경(차가운 회백색) |
| `--white` | `#ffffff` | 카드, 그림 바탕, 버튼 바탕 |
| `--ink` / `--ink-soft` | `#0f1b3d` / `#55628a` | 글자, 굵은 테두리 / 보조 글자 |
| `--rule` | `#cbd3ea` | 얇은 구분선, 카드 테두리 |
| `--unit` / `--unit-tint` | `#2d3fe0` / `#e3e7ff` | **단원 색**: 띠, 활성 버튼, 게이지, 강조 |
| `--plus` / `--minus` | `#d9304c` / `#0b8f68` | 양성자(+) / 전자(−) |
| `--serif` / `--sans` | Noto Serif KR / IBM Plex Sans KR | 제목·숫자·설명 / 본문·UI |

- 시뮬레이션마다 바꾸는 것은 **단원 색(`--unit`, `--unit-tint`)과 단원 띠 글자**뿐이다. 나머지 토큰은 그대로 쓴다. 새 색, 새 글꼴을 만들지 않는다.
- Canvas와 SVG 안의 색·글자도 같은 토큰에 맞춘다(`main.js`의 `COLOR` 상수, 글자는 `#0f1b3d`, 글꼴은 IBM Plex Sans KR). 바탕은 흰색이다.

### 화면 구성: 상단 / 중앙(좌·우) / 하단

1. **상단**
   - 왼쪽 세로 **단원 띠**(`--unit` 바탕, 흰 세로 글자). 휴대폰에서는 위 가로 띠.
   - 머리글: 질문형 큰 제목(세리프 `h1`) + 한 줄 설명. 실험이 바뀌면 제목도 바뀐다.
   - 실험 선택 버튼(`.mode-tab`)과 "다시 하기"(`.btn-pop`).
   - **실험 안의 모든 선택 버튼(과정, 보기)은 상단에 한 줄로 나열**한다(`.steps`). 순서가 있으면 번호 원(`.n`)을 쓰고, 없으면 ①②③ 같은 글머리만 쓴다. 오른쪽 패널 안에 버튼을 두지 않는다.
2. **중앙** (1000px 이상 좌 7 : 우 5)
   - 왼쪽: 시뮬레이션 그림(Canvas) 카드(`.figure`). 위에 얇은 게이지, 아래에 그림 설명(`figcaption`), 조작 안내 말풍선(`.hint`).
   - 오른쪽: 전하·결과 그림(SVG) + 수치표(`.net-cards`) + 상태 표시(칩 `.chip`, 관찰 기록 `.obs-log`).
3. **하단**
   - **설명 텍스트는 항상 그림 아래에 가로로 넓게**(`.explain > .caption`, 인디고 옅은 바탕 + 왼쪽 단원 색 줄 + 세리프). 설명을 오른쪽 패널에 넣지 않는다.
   - 그 아래 푸터 크레딧(`.footer`)과 제작자 모달.

### 버튼 형태 (B안)

- 3px 잉크 테두리, 모서리 14px, 그림자 `4px 4px 0 var(--ink)`. 누르면 `translate(3px, 3px)`와 그림자가 줄어든다.
- 활성 = 단원 색 바탕 + 흰 글자. 비활성 = 흰 바탕 + 잉크 글자.
- 높이 48px 이상(태블릿 손가락 조작), `touch-action: manipulation`, 키보드 포커스는 4px 단원 색 외곽선.
- 그림자가 있는 요소는 **버튼과 크레딧 링크, 모달 닫기 버튼뿐**이다. 카드와 패널은 그림자와 둥근 모서리 없이 얇은 `--rule` 테두리의 평평한 흰 카드로 한다.
- 상태 표시(칩, 태그)는 버튼처럼 보이면 안 되므로 그림자 없이 얇은 테두리만 쓴다.

### 반응형 (태블릿 기본)

- 기본(700~999px, 태블릿 세로): 왼쪽 단원 띠 + 1열(그림, 전하 패널, 설명 순서).
- 1000px 이상(태블릿 가로, 노트북): 2단 + 왼쪽 그림 고정(`sticky`), 그림은 화면 높이에 맞춰 줄어든다. 높이 900px 이하이면 머리글을 줄인다.
- 700px 미만(휴대폰): 단원 띠가 위 가로 띠, 버튼은 세로로 쌓는다. 어떤 크기에서도 가로 스크롤이 없어야 한다.

### 공통 컴포넌트 (새로 만들지 말고 재사용)

`.mode-tab`, `.btn-pop`, `.steps`(`.step-tab`, `.tab`), `.figure`, `.gauge-card`/`.gauge`, `.stage`, `.hint`, `.net-cards`, `.chip`, `.tag`, `.obs-log`, `.caption`, `.footer`/`.credit-link`, `.modal`.

### 금지

- 다크 테마, 그림자가 있는 큰 둥근 카드, 그라데이션 장식, 토큰에 없는 색과 글꼴
- 소문자 라벨을 영문 대문자로 만든 장식, 가운뎃점(·)으로 이은 문구, 의미 없는 번호 매기기
- 색만으로 구분하는 표시(양성자·전자는 색과 기호 (+)(−)를 함께 쓴다)

### 접근성

- 버튼과 링크에 키보드 포커스가 보여야 한다. 모달은 `role="dialog"`, `aria-modal="true"`, 닫기 버튼 `aria-label`.
- `prefers-reduced-motion`이면 전환 효과를 끈다. 그림에는 `aria-label`을 단다.

## 개념 정확성 (과학 콘텐츠)

- 개념은 명확해야 한다. 학생에게 오개념을 줄 수 있는 표현, 배치, 설명 문구는 만들지 않는다.
- 최대한 과학적 원리를 올바르게 구현한다. 교육용으로 단순화가 필요하면 원리를 왜곡하지 않는 선에서 하고, 단순화한 부분은 코드 주석이나 문서에 남긴다.

## 작업 흐름

1. 새 기능이나 구조 변경은 `superpowers:brainstorming`으로 설계를 짧게 제시하고 승인받은 뒤 구현한다. 사용자가 방향을 명확히 지시한 작은 수정은 바로 진행해도 된다.
2. 구현 후 브라우저에서 직접 확인한다(아래 체크리스트).
3. 커밋 메시지는 한국어, `feat:` / `fix:` / `docs:` 접두어. 커밋 끝에 `Co-Authored-By: Claude Sonnet 5 <noreply@anthropic.com>`.
4. `main`에 push하면 Vercel이 자동 배포한다. 배포는 `list_deployments`로 READY를 확인하고, 라이브 URL에서 다시 확인한다.
5. 새 시뮬레이션을 추가할 때: 루트에 폴더 생성 → 같은 디자인 토큰, 푸터, 크레딧 모달 재사용 → Vercel 프로젝트를 새로 만들어 Root Directory를 그 폴더로 지정 → 루트 `README.md` 표에 추가.

## 검증 체크리스트

- [ ] 개념이 명확하고 과학적으로 올바른가 (오개념을 줄 표현이나 화면이 없는가)
- [ ] 다시 하기로 처음 상태 복귀
- [ ] 마우스와 터치 모두 동작 (`touch-action: none`)
- [ ] 콘솔 에러 없음, 어떤 폭에서도 가로 스크롤 없음
- [ ] 디자인 기본값 준수: 토큰만 사용, 상단/중앙/하단 구성, 설명은 하단, 선택 버튼은 상단 한 줄, 버튼 형태
- [ ] 태블릿 세로(820×1180)와 가로(1180×820)에서 확인, 휴대폰(375)에서 깨지지 않는지 확인
- [ ] 푸터 크레딧과 모달 유지(재배포 시 출처 표기 유지 조건), 모달 열고 닫아도 시뮬레이션 상태 불변
- [ ] push 후 배포 READY, 라이브 URL 동작

## 미리보기 검증 요령

- 로컬 서버: `preview_start`의 `friction-electricity`(포트 5173), 시안은 `design-proposals`(포트 5174). 둘 다 `python -m http.server`.
- 화면 크기는 `resize_window`로 태블릿 세로 820×1180, 가로 1180×820, 휴대폰 375×812를 확인하고, 끝나면 `desktop` 프리셋으로 되돌린다.
- 코드를 고친 뒤에는 `?v=숫자`를 붙여 다시 열거나 강력 새로고침한다. 안 하면 이전 스크립트가 캐시되어 헷갈린다.
- 가려진 탭에서는 `requestAnimationFrame`이 느리다. 상태를 코드로 직접 진행시켜 검증하고, 스크린샷은 한 번 더 찍어 확인한다.
- 실제 드래그가 필요하면 캔버스에 `PointerEvent`(`pointerdown`, `pointermove`, `pointerup`)를 발생시킨다.

## 자동화 (훅)

- `.claude/settings.json`의 PostToolUse 훅: Edit/Write로 `.js` 파일을 고치면 `.claude/hooks/check-js.mjs`가 `node --check`로 문법을 검사한다.
  - 문법 오류면 exit 2로 Claude에게 오류를 돌려주어 바로 고치게 한다. 정상이거나 `.js`가 아니면 조용히 넘어간다.
  - 새 세션에서 훅이 안 뜨면 `/hooks`를 한 번 열거나 세션을 다시 시작한다.

## 하지 말 것

- 요청하지 않은 기능(점수, 퀴즈, 로그인 등) 추가
- 기존 시뮬레이션 폴더 구조나 배포 설정을 사용자 확인 없이 변경
- repo 생성, 공개 범위 변경, 배포 대상 변경은 사용자에게 확인한 뒤에만
- 크레딧 문구와 모달 삭제(출처 표기 유지가 이용 조건)
